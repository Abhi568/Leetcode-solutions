class MyQueue:
    # stack = LIFO
    # Q = FIFO
    
    #[ 1 2 3] -> 3 stack
    #[1,2,3,4] -> 1 Queue
    
    def __init__(self):
        self.q = MyQueue()
        
    def push(self, x: int) -> None:
        self.q.push(x)

    def pop(self) -> int:
        return q.stack.pop()

    def peek(self) -> int:
        return self.q.peek()

    def empty(self) -> bool:
        return self.q.empty()
        


# Your MyQueue object will be instantiated and called as such:
# obj = MyQueue()
# obj.push(x)
# param_2 = obj.pop()
# param_3 = obj.peek()
# param_4 = obj.empty()
